#include<iostream>
#include<cstring>
using namespace std;

int main()
{
    // string s;
    
    // int x;
    // cin>>x;
    // getline(cin>>ws,s);
    // cout<<x<<" "<<s;
    string s{"vISHNU"};

    // uint32_t a {15};
    char cs[] {"Vishnu"};

    // size_t len = s.length();
    size_t len = strlen(cs);
    cout<<len;

    
     
}